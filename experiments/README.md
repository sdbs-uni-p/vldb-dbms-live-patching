
System optimizations:
```
# Set CPU frequency to max. (even when idle)
sudo cpupower frequency-set -g performance

# Perform experiments inside VM

# Reset CPU frequency 
sudo cpupower frequency-set -g schedutil
```
